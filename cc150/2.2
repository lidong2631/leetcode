see leetcode remove nth node from end of list 迭代解法

递归解法如下


int count = n;

public LinkedListNode nthToLast(LinkedListNode head) {
	if(head==null)
		return;
	nthToLast(head.next);
	if(count==1)
		return head;
	count--;
}

Note: 注意下recursion VS iteration解法的优缺点